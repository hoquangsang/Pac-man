# from utils.datastructure.node import Node
# from utils.datastructure.graph import Graph

# def contructPath(graph: Graph, fromNode: Node, toNode: Node) -> list[Node]:
#     if graph is not None:
#         path: list[Node] = []
#         current = Graph[fromNode]

#         while current:
#             path.append(current)
#             current = Graph[current]

