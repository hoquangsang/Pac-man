from .edge import Edge
from .node import Node

class Graph(object):
    def __init__(self):
        self.edgeList: list[Edge] = [] #Danh sách kề

    def contructCameFrom(came_from: dict[Node, Node]):
        for node in came_from.values():
            pass
        
    def clear(self):
        self.edgeList.clear()
